ADD you_certificate.crt:/container/cert/path
RUN update-ca-certificates



docker run -v /host/path/to/certs:/container/path/to/certs -d IMAGE_ID "update-ca-certificates"



keytool to create a key locally 
keytool -genkey -alias vamsi -keyalg RSA -keystore "c:\local.keystore"


keytool -v -list -keystore aa.keystore

https://winstonkotzan.com/blog/2019/03/09/production-https-setup-for-ruby-on-rails-app-with-docker.html

$ sudo apt-get update
$ sudo apt-get install software-properties-common
$ sudo add-apt-repository universe
$ sudo add-apt-repository ppa:certbot/certbot
$ sudo apt-get update
$ sudo apt-get install certbot python-certbot-nginx


sudo certbot --nginx -d nginx-demo.abcjjajja.com


https://github.com/rchidana/Docker-Private-Registry/


subjectAltName=IP:35.192.220.208
docker tag hello-world 35.192.220.208:5000/hello-world

docker push 35.192.220.208:5000/hello-world


sudo mkdir -p /etc/docker/certs.d/35.192.220.208:5000

sudo cp /certificates/domain.crt /etc/docker/certs.d/35.192.220.208:5000/ca.crt

sudo ls /etc/docker/certs.d/35.192.220.208:5000/
